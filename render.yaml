services:
  - type: web
    name: denesens-solutions
    runtime: docker
    plan: free
    envVars:
      - key: MONGODB_URI
        sync: false
      - key: ADMIN_SECRET
        sync: false
      - key: CLIENT_URL
        sync: false
